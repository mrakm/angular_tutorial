// import { DashboardRoutes } from 'src/app/client/dissolution/dashboard-routes.enum';

export interface DashboardStep {
  label: string;
  completed: boolean;
  index: number;
  // route: DashboardRoutes;
}
